#ifndef PARSER_HPP_INCLUDED
#define PARSER_HPP_INCLUDED

/// TD
/*
    FSA parser
    CFG parser
    PDA parser
    ...
*/
/*
    escape sequence: backslash -> the "monos": \\ \n \r \t \n  and the "numeric": \123 \077 \377 \777

*/
/*
    algebra: there needs to be a clear difference between the different (*!?) "variants of sameness": "equivalence" (math), "equality" (comparison),
        "assignment" (code), "isomorphism" (...?), "homotopy" (...?), "substitutability" (...?), "equivalence ... property" (!?)[<e.g.> Liskov
        substitution principle], "congruency" (~ "equality" in modulo spaces), "identity" (...?), "identical" (...?), "by definition / defined as..." (...?),
         ...

*/
#endif // PARSER_HPP_INCLUDED
