#ifndef FUNCTIONANALYZER_HPP_INCLUDED
#define FUNCTIONANALYZER_HPP_INCLUDED

/*
    analyze how much of a function is "serial" and how much of it can be done "parallely"
    (*?) imagine the whole ceiling being filled with a function (written with a low-size font); that function is the
        decision function of an AI; the AI has decided to go rogue and try to "break the computer interface" in an
        attempt to "find holes in the interface's impure function"
*/

#endif // FUNCTIONANALYZER_HPP_INCLUDED
