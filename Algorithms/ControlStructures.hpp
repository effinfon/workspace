#ifndef CONTROLSTRUCTURES_HPP_INCLUDED
#define CONTROLSTRUCTURES_HPP_INCLUDED

/*
    a switch statement that works with strings
    a ~ "parallel for" or "multi-iterator for", where the iterators can change at different rates, start at different points
        and be different things (and also each be composable with its own separate modulo space; or they could share the
        modulo space)


*/

#endif // CONTROLSTRUCTURES_HPP_INCLUDED
