#ifndef SCANNER_HPP_INCLUDED
#define SCANNER_HPP_INCLUDED

/*
    functionality: rescan the previousely scanned page, or the i-th scanned page from the file
    functionality: selectable pages into files; mechanism to possibly scan many files and then create multiple
        files from (possibly) different pages

*/

#endif // SCANNER_HPP_INCLUDED
