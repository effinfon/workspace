#ifndef COMPILINGTRACKING_HPP_INCLUDED
#define COMPILINGTRACKING_HPP_INCLUDED

/// TD
/*
    tracks which files have been compiled, which filse have changed, which files needs to be recompiled
        and due to what rows; it can also check which "blocks" need to be recompiled (depending on the
        unit of modularizaiton)
*/

#endif // COMPILINGTRACKING_HPP_INCLUDED
