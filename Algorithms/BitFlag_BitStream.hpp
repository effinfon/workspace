#ifndef BITFLAG_BITSTREAM_HPP_INCLUDED
#define BITFLAG_BITSTREAM_HPP_INCLUDED

/// TD
/*
    since they often tend to be useful, let's properly define a type for these; it will start by using
        uint8_t all the way up to uint64_t, after which it will add another uint8_t which will grow all
        the way up to uint64_t, and so on

*/

#endif // BITFLAG_BITSTREAM_HPP_INCLUDED
